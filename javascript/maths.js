// math is a build in funnction that provide a collective of maths properties
// the function starts with Math followed by dot operetor
// 
let a =1
let b=2;
let c=92;
let e=81;
//to round a number 
//exampe
let d=56.48;
let f=7.888
d=Math.round(d)
console.log(d); //this rounds the number to 57
// to round a number down use math.floor fuction
d=Math.floor(d)
console.log(d); //this rounds the number to 57
// when you neeed pi
Math.PI
console.log(Math.PI)
//to round-up use Math.ceil
d=Math.ceil(d)
console.log(d)
//to truncate  the decimal part of a fraction, use Math.truct()
f=Math.trunc(f)
console.log(f)
// raising any number to a base
f=Math.pow(f,a)
console.log(f)
// square root of anumber
e=Math.sqrt(e)
console.log(e)
// to find the maximum of any given numbers we use the max funtion
let max=Math.max(a,b,c,e)
console.log(max)
//use the math.min for the minimun
let min=Math.min(a,b,c,e)
console.log(min)

